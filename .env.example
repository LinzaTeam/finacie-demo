# Демо-данные используются только как fallback при недоступном GET /api/v1/dashboard.
VITE_FINANCE_DEMO=false

# Только для локального dev-auth. В production переменная не задаётся.
VITE_FINANCE_DEV_USER=local-reviewer
